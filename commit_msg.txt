Fix login redirect loop, social feed inconsistency, and nav cleanup

- layout.tsx + sign-in page: change fallback redirect to '/' (was '/studio'
  which caused a loop if studio required auth before session was ready)
- sign-in page: add forceRedirectUrl='/' so Clerk always lands at home after login
- social/page.tsx: replace SocialPageContent with SocialFeed for consistency
  with home page and dashboard social tab
- DashboardCards.tsx: social tab now uses SocialFeed (embedded) instead of
  SocialPageContent; removes duplicate dynamic import
- dashboard-data.ts: Jarvis dock entry href updated from '#' to '/agent'

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
