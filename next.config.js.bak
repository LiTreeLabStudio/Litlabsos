import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable Turbopack root discovery which might cause /data/data scanning
  
  experimental: {
    // serverActions: true,
  },
  
  images: {
    unoptimized: true,
  },
  
  // Force Webpack
  webpack: (config) => {
    config.watchOptions = {
      poll: 1000,
      aggregateTimeout: 300,
    };
    return config;
  },
};

export default nextConfig;
